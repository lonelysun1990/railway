{
  "name": "railway-frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "serve dist -l tcp://0.0.0.0:${PORT:-3000}"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}
